import { useActionState } from "react"

function AddToCart({ itemId, itemTitle }) {
  const [formState, formAction] = useActionState()
  return (
    <form>

    </form>
  )
}

export default function AddToCartAnother() {

}